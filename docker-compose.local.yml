version: '3.8'

services:
  # Python Backend Service
  backend:
    build: 
      context: ./automationtool
      dockerfile: Dockerfile
    container_name: makereels-backend-local
    restart: unless-stopped
    environment:
      - PORT=8000
      - PYTHONPATH=/app
    volumes:
      - ./automationtool/input:/app/input
      - ./automationtool/output:/app/output
      - ./automationtool/config:/app/config
      - ./automationtool/logs:/app/logs
    networks:
      - makereels-local-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # React Frontend Service (Local)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.local
    container_name: makereels-frontend-local
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - makereels-local-network
    environment:
      - REACT_APP_API_URL=http://backend:8000

networks:
  makereels-local-network:
    driver: bridge
